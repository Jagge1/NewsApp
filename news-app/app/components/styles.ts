import styled from 'styled-components/native'
import { View } from 'react-native';

export const Colors = {
  primary: "#ffffff",
  secondary: "#E5E7EB",
  teriary: "#1F2937"
}

const { primary, secondary, teriary} = Colors;

export const StyledContainer = styled.View`
  flex: 1;
`;